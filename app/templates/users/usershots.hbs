<section class="usershots">
	<div class="container-fluid">
		<h4>Shots.</h4>
		<div class="row">
		{{#each model as |shot|}}      
			<div class="col-sm-4">
				<img src="{{shot.images.teaser}}">
			</div>
		{{/each}}
		</div>
	</div>
</section>